apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: dripcraft-gateway
spec:
  gatewayClassName: my-tcp-gateway-class
  listeners:
  - name: dripcraft-default
    protocol: TCP
    port: 25565
    allowedRoutes:
      kinds:
      - kind: TCPRoute