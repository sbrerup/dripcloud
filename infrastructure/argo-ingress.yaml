apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dripcd
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - dripcd
  rules:
    - host: dripcd
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 80
