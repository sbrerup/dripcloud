machine:
  network:
    interfaces:
      - interface: eth0
        addresses:
          - 10.1.2.90/24
        routes:
          - network: 10.1.2.0/24
            gateway: 10.1.2.1
  install:
    disk: /dev/sda
cluster:
  allowSchedulingOnControlPlanes: true
